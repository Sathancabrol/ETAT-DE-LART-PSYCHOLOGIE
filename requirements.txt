# Dependances applicatives (FastAPI + SQLite + Jinja2)
# Installation : pip install -r requirements.txt
# Lancement : uvicorn app.main:app --host 0.0.0.0 --port 8000
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
pandas>=2.0
# Note : scripts/github_inventory.py n'utilise que la bibliotheque standard.
